spring:
  cloud:
    config:
      enabled: false
  main:
    allow-bean-definition-overriding: true
embedded:
  postgresql:
    enabled: false
  redis:
    enabled: false
  kafka:
    enabled: false
    file-system-bind:
      enabled: false
  zookeeper:
    enabled: false
    file-system-bind:
      enabled: false